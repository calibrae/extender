#ifndef ExtenderVirtualNetwork_h
#define ExtenderVirtualNetwork_h

#include <Availability.h>
#include <DriverKit/IOService.iig>

// Note: This should inherit from IOUserNetworkEthernet (NetworkingDriverKit) once
// that framework is linked in the Xcode project. For now, stub as IOService.
class ExtenderVirtualNetwork : public IOService
{
public:
    virtual bool init() override;
    virtual kern_return_t Start(IOService * provider) override;
    virtual kern_return_t Stop(IOService * provider) override;
};

#endif /* ExtenderVirtualNetwork_h */
