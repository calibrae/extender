#ifndef ExtenderVirtualAudio_h
#define ExtenderVirtualAudio_h

#include <Availability.h>
#include <DriverKit/IOService.iig>

// Note: This should inherit from IOUserAudioDevice (AudioDriverKit) once that
// framework is linked in the Xcode project. For now, stub as IOService.
class ExtenderVirtualAudio : public IOService
{
public:
    virtual bool init() override;
    virtual kern_return_t Start(IOService * provider) override;
    virtual kern_return_t Stop(IOService * provider) override;
};

#endif /* ExtenderVirtualAudio_h */
